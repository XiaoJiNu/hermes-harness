# Handoff: <workstream>

日期：YYYY-MM-DD
From：
To / next owner：
状态：ready | blocked | paused
建议保存路径：`docs/plans/active/<plan-id>-handoff-<YYYY-MM-DD>.md`

## Objective

一句话描述正在到达的目标状态。

## Canonical artifacts

只链接，不复制已有内容：

- Spec：
- Active plan：
- Decisions：
- Domain glossary：
- Relevant diff / issue：

## Completed

- `<completed item>` — evidence: `<path/command/result>`

## Current state

- 当前分支/工作区状态：
- 当前 frontier 或 in-progress item：
- 已验证事实：
- 尚未验证的假设：

## Remaining work

| Item | Status | Blocked by | Acceptance | Verification |
| --- | --- | --- | --- | --- |
| `<item>` | ready | - | `<criterion>` | `<command>` |

## Decisions and constraints

- `<decision>` — source: `<ADR/spec>`

## Blockers / risks

- `<blocker>` — owner /解除条件：`<owner/condition>`

## Verification already run

| Command | Result | Date |
| --- | --- | --- |
| `<command>` | `<key output>` | YYYY-MM-DD |

## Exact next action

`<one concrete action that resumes progress>`

## Safety

- 不写入 token、密码、cookie 或其他凭据。
- 敏感值统一记为 `[REDACTED]`。
- 临时日志只保留恢复工作所需的最小信息。
